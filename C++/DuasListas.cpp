#include <iostream>
#include <list>
using namespace std;
int main()
{
	list<int> lista1;
	list<int> lista2;
	list<int> lista3; // ponteiro para a lista ligada
	int x;
	while(x != 0)
	{
		cin >> x;
		if(x != 0)
		{
			lista1.push_back(x);
		}
	}
	while(x != 0)
	{
		cin >> x;
		if(x != 0)
		{
			lista2.push_back(x);
		}
	}
	for
	
	return 0;
}